# 9 月 2 日联调总结与复盘

## 最终结果

今天完成了手机蓝牙到电机执行的整条链路：

```text
LightBlue/FFE1
  → JDY-16
  → Hi3861 UART1（9600）
  → 6 字节电机帧
  → Hi3861 UART2（115200）
  → STM32 USART1
  → PID + PWM
  → 左右电机
```

最终实测证据：手机发送 `W` 时串口输出 `BLE RX byte=0x57`、`CMD w: hold (L=150 R=150)`，车轮成功转动；发送 `0` 时输出 `BLE RX byte=0x30`、`CMD stop`，车辆停止。

## 今天解决的问题

### 1. Hi3861 镜像没有包含小车应用

最早烧录虽然显示 `Execution Successful`，但启动日志只有 HarmonyOS/SAMGR 系统服务，且显示应用服务数量为 0，没有任何小车初始化输出。根因不是烧录失败，而是应用目录没有正确加入上层 `app/BUILD.gn`，重新编译出来的 BIN 仍可能是不含业务代码的镜像。

改进：以后判断“编译和烧录是否成功”必须同时核对三件事：构建目标是否包含应用、BIN 时间戳/哈希是否变化、启动日志是否出现应用自己的版本标记。

### 2. 错把另一套项目架构当成本车硬件事实

期间参考了另一组的 GitHub 方案：GPIO11 软件 UART、两端 9600、10 字节 V2 帧。该方案在对方车辆上可能成立，但不能证明本车使用同样的 STM32 固件和协议。按这套假设修改后，蓝牙和电机仍不通，增加了重复编译与烧录。

老师提供的参考代码给出了本车更直接的证据：

- UART1：GPIO0/GPIO1，9600，连接蓝牙；
- UART2：GPIO11/GPIO12，115200，连接 STM32；
- 电机帧：`FC DIR SPEED DIR SPEED FD`，共 6 字节；
- STM32 速度范围：0～150。

改进：涉及板级串口、引脚和私有协议时，优先级应是本车原理图/老师代码 > 本车串口实测 > 同型号公开资料 > 别组项目，不能反过来。

### 3. 混淆了两段串口的波特率

系统里有两条独立串口链路，参数并不相同：蓝牙 UART1 是 9600，双核通信 UART2 是 115200。把两边都改成 9600 会让 STM32 无法按老师固件的参数解帧；把手机调试串口的 115200 当成蓝牙模块速率也会干扰判断。

改进：以后用“端点 A ↔ 端点 B”的方式记录串口参数，不再只说笼统的“串口波特率”。

### 4. UART1 初始化成功不等于应用能收到蓝牙数据

日志曾显示 UART1 初始化返回 0，但手机发送命令没有任何字节日志。进一步检查 SDK 发现 AT 模块同样初始化并占用 UART1，应用虽能初始化串口，接收数据仍可能被 AT 任务读取。

解决方法：关闭 `CONFIG_AT_COMMAND` 或禁止 `hi_at_init()`，由小车应用显式反初始化并重新初始化 UART1。加入原始字节日志后，终于看到 `BLE RX byte=0x57`，证明手机→JDY-16→Hi3861 链路真实打通。

改进：驱动初始化返回成功只证明配置调用成功，不能证明数据归当前任务所有；必须用原始 RX 字节作为接收链路证据。

### 5. 手机 Hex/ASCII 格式不是主要故障

一度怀疑 LightBlue 的输入格式。实际 `W` 的 ASCII 字节就是 Hex `57`，停车 `0` 是 Hex `30`；日志收到 `0x57` 后证明手机写入特征 FFE1 的操作正确。

改进：应用层输入问题用“接收端打印原始字节”一次判定，不应反复切换 App、特征值和换行格式。

### 6. 串口链路通了，但电机只有声音不转

收到 `W` 后电机持续发声、收到 `0` 后声音变小，说明命令已经影响到后级，但轮子没有克服静摩擦启动。检查老师原始电机示例后发现其 PWM 使用 `PWM_Init(7199, 9)`，直接测试值约为 4000；当时 PID 首次输出偏小，且 PWM 分频参数与示例不一致。

最终修改：

- 恢复 `PWM_Init(7199, 9)`；
- 非零速度目标加入带方向的 `±3600` 启动补偿；
- 保留 PID 调节和输出限幅；
- STM32 重新编译结果为 0 错误、0 警告。

重新烧录 STM32 后，车轮成功启动。这说明最后一个主要问题位于电机输出强度，而不是蓝牙输入。

### 7. COM4 被占用是电脑端工具冲突

HiBurn 报 `Com4 open fail`，串口助手同时报“串口不存在或者被其他应用程序占用”，这是两个 Windows 程序争用同一个 COM 口，不是固件错误。烧录前必须关闭串口助手的端口；查看日志前必须让 HiBurn 断开。

## 下次应采用的排查顺序

1. **确认运行的是新镜像**：核对时间戳、MD5 和应用启动标记。
2. **确认手机写入**：目标设备是 JDY-16、目标特征是 FFE1、接收端打印原始字节。
3. **确认 Hi3861 解析**：检查 `BLE RX byte` 和 `CMD` 日志。
4. **确认双核参数**：UART2/USART1 两端都为 115200，协议都为同一套 6 字节帧。
5. **确认 STM32 执行**：先悬空测试，观察命令前后声音、灯和轮子是否变化。
6. **最后查动力部分**：电池、PWM、启动补偿、电机驱动和堵转情况。

每一步只验证一段链路。上一步没有证据通过时，不应同时修改下一步的协议、波特率和算法。

## 当前尚未完成的工作

- 防跌落状态机已经实现，但底部 GPIO13/GPIO14 探头的真实极性尚未逐个验证。
- `G` 防跌落模式尚未在桌沿完成安全实测。
- GPIO7/GPIO8 超声波避障模式尚未实测。
- 左转、右转、后退和定时命令与前进使用同一条已打通链路，但仍应逐项做实车验收。
- 停车后的轻微电气声需要继续观察；只要轮子停止且器件不发热可进一步排查，若持续强烈嗡鸣或升温应立即断电。

## 最重要的经验

这次最大的弯路不是某一行代码，而是在证据不足时同时更换了协议、串口实现和波特率，使故障层级变得不可见。最终有效的方法是加入可观察日志、恢复老师代码所证明的硬件参数，并按“蓝牙输入 → Hi3861 解析 → 双核帧 → STM32 PWM”逐层闭环验证。

